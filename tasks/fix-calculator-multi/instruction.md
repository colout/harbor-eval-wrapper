turns:
  - prompt: |
      The calculator module in `/workspace/calculator.py` has a bug in the
      `multiply` function. It currently returns the wrong result.

      Fix the bug so that multiply(a, b) returns the product of a and b.

  - prompt: |
      Now add a `power` function to `/workspace/calculator.py` that raises
      a number to a given exponent.

      The function signature should be: power(base, exponent)

      For example:
      - power(2, 3) should return 8
      - power(5, 0) should return 1
