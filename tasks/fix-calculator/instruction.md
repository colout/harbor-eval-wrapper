The calculator module in `/workspace/calculator.py` has a bug in the `multiply` function.

The `main.py` file uses this calculator and is currently producing incorrect results.

Fix the bug in `calculator.py` so that all operations work correctly.

Do not modify `main.py`.
